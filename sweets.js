function sweets(n) {
    if (n % 2 == 0) {
        console.log("yes it can be distributed");
    }
    else {
        console.log("no it cannot be distributed")
    }
}

sweets(8);
sweets(7);